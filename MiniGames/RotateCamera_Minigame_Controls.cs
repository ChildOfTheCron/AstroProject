using UnityEngine;
using System.Collections;

public class RotateCamera_Minigame_Controls : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if (Input.anyKey){
			//target.texture = Resources.Load("Balance/Cntrollers", typeof(Texture2D));
		}
	}
}
